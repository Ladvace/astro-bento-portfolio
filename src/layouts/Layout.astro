---
import BasicLayout from "./BasicLayout.astro";

interface Props {
  title: string;
  description: string;
  page?: "travel";
  fullScreen?: string;
  hideSoundToggle?: boolean;
}

const { title, description, hideSoundToggle } = Astro.props;
---

<BasicLayout
  description={description}
  title={title}
  hideSoundToggle={hideSoundToggle}
>
  <slot />
</BasicLayout>
